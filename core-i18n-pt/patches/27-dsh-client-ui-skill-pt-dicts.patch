--- a/dsh-client-ui-skill/lib/client.js
+++ b/dsh-client-ui-skill/lib/client.js
@@ -204,6 +204,14 @@
 			"row.instructions": "Instructions",
 			"menu.userOnly": "user-only"
 		};
+		const pt = {
+			"row.running": "Carregando habilidade",
+			"row.failed": "Falha ao carregar a habilidade",
+			"row.stopped": "Carregamento da habilidade interrompido",
+			"row.instructions": "Instruções",
+			"menu.userOnly": "somente do usuário",
+		};
+
 		//#endregion
 		//#region lib/types/client/index.js
 		/** Required services: reference source faces plus the tool-row and locale registries. */
@@ -222,6 +230,7 @@
 		function apply(ctx) {
 			ctx.effect(() => ctx.locale.register(NS, {
 				zh,
+				pt,
 				en
 			}), "ui-skill: dictionaries");
 			ctx.slots.inject("tool.call.toolview", () => ctx.slots.register({
