--- a/dsh-client-ui-reference/lib/client.js
+++ b/dsh-client-ui-reference/lib/client.js
@@ -43,6 +43,15 @@
 			"candidate.session": "Session",
 			"candidate.noCwd": "(no cwd)"
 		};
+		const pt = {
+			"section.files": "Arquivos e pastas",
+			"section.sessions": "Conversas da sessão",
+			"candidate.file": "Arquivo",
+			"candidate.folder": "Pasta",
+			"candidate.session": "Sessão",
+			"candidate.noCwd": "(sem cwd)",
+		};
+
 		//#endregion
 		//#region lib/types/client/index.js
 		/** Required services: the trigger registry, the Remote namespaces, and the copy. */
@@ -60,6 +69,7 @@
 		function apply(ctx) {
 			ctx.effect(() => ctx.locale.register(NS, {
 				zh,
+				pt,
 				en
 			}), "ui-reference: dictionaries");
 			const t = ctx.locale.bind(NS);
